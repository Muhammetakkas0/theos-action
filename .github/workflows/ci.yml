name: CI
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Theos
        uses: randomblock1/theos-action@v1

      - name: Build Tweak
        run: |
          sed -i '1d' Tweak.x || true
          make package FINALPACKAGE=1
          
      - name: Upload Result
        uses: actions/upload-artifact@v3
        with:
          name: Akgram-Final
          path: packages/*.deb
